#include "user.h"


// Puclic functions
bool user::authenticate(std::string login) {
    //compare user login with userPermissions.txt, if present allow acccess else deny
}

std::string user::borrowItem() {
    // if item present in locker, loan item, else, print empty
}
